{
  "name": "claudenomics-cli",
  "version": "0.1.0",
  "description": "Transparent wrapper around claude-code and codex with token accounting.",
  "type": "module",
  "bin": {
    "claudenomics": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@claudenomics/logger": "*",
    "@claudenomics/proxy": "*",
    "@claudenomics/usage": "*",
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
