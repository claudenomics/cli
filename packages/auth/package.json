{
  "name": "@claudenomics/auth",
  "version": "0.1.0",
  "description": "Browser-loopback login + local session storage for the claudenomics CLI.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@claudenomics/api": "*",
    "@claudenomics/logger": "*",
    "@napi-rs/keyring": "^1.1.5",
    "jose": "^5.9.6",
    "proper-lockfile": "^4.1.2"
  },
  "devDependencies": {
    "@types/proper-lockfile": "^4.1.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
